/**
 * Storage Client
 *
 * Re-exports from s3Storage.js (S3 with local filesystem fallback).
 * This file exists for backward compatibility with existing imports.
 */

module.exports = require('./s3Storage');
